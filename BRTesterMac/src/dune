(executable
 (name app)
 (libraries brisk_cocoa brisk_cocoa_menu cohttp.lwt)
 (flags (:standard
        (-runtime-variant d -verbose -g)
 ))
 (modes object))

(install
  (package brisk)
  (section bin)
  (files (app.exe.o as app.o)))

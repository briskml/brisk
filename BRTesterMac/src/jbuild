(jbuild_version 1)

(executable
 ((name app)
 (libraries (brisk_cocoa))
 (flags (:standard -runtime-variant d -verbose -g))
 (link_flags (:standard
  (-ccopt -g)
  (-ccopt "-framework Cocoa")
 ))
 (modes (exe))
))

(install
  ((package brisk)
  (section bin)
  (files (app.exe)))
)
